#target photoshop

var doc = app.activeDocumsASaent;
var layer = doc.activeLayer;
var group = layer.parent.layers;

//var newLayer = activeDocument.artLayers.add();
// Create a new ArtLayer object newLayer.name = "My Layer";  name it for later reference
//  newLayer.name = "Test5";
//
//var layerRef = app.activeDocument.artLayers.getByName("Test");
//layerRef.blendMode = BlendMode.OVERLAY
//layerRef.fillOpacity = 50
// d
//layerRef.duplicate = true;
//app.activeDocument.activeLayer.duplicate();

var fill50 = app.activeDocument.artLayers.getByName("Fill Me");
fill50.blendMode = BlendMode.SOFTLIGHT;
fill50.fillOpacity = 20;

var document = app.activeDocument.ChangeMode();
colorspace.ChangeMode = CMYKMode
document.changeMode().
//  (CMYK[, options])

function swtichlayer();
layer.visible = false;
doc.activeLayer = group[ife]
}
activeDocumsASaent

function
switch

var testMe = app.activeDocument

function switchlayer() {}
